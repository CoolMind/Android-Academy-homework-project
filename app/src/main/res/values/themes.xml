<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Шрифты Roboto -->
    <style name="RobotoWeight300" parent="AppTheme">
        <item name="android:fontFamily">sans-serif</item>
    </style>

    <style name="RobotoWeight900" parent="AppTheme">
        <item name="android:fontFamily">sans-serif-black</item>
    </style>

    <!-- ShapeableImageView -->
    <style name="MovieActorImageView.CornerRadius" parent="AppTheme">
        <item name="cornerSize">@dimen/movie_actor_corner_radius</item>
    </style>

    <style name="MovieThumbnailImageView.CornerRadius" parent="AppTheme">
        <item name="cornerSize">@dimen/movie_thumbnail_corner_radius</item>
    </style>
</resources>